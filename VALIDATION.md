# 验证记录 · 2026-09-22

本文件描述实际完成的验证，不把代码存在等同于真实硬件验收。

## 环境与构建

- macOS 15.7.1，Apple Silicon，Xcode / Swift 6.2.4，Swift 5 语言模式。
- PASS：`swift test`，31 项 XCTest，0 失败（v0.2 时间轴扩展）。详见 docs/evidence/timeline-tests.log。
- PASS：`./scripts/build-app.sh`，release 编译、标准 `.app` 目录、应用图标、本机 ad-hoc 签名及签名校验。
- PASS：最终打包应用已通过原生界面启动，重新打开保存的示例工程并生成预览。
- 本轮最终编译与测试日志未出现 warning/error。

## 自动化行为测试

| 范围 | 证据 / 结果 |
|---|---|
| 删除中间片段 | 成片时间正确映射回剩余源时间；切点无插值跨越 |
| 选区与撤销重做 | 跨多个保留片段的选区正确，历史恢复正确 |
| 自动聚焦 | 点击合并、无点击不触发、边缘裁剪保持在画面内 |
| 坐标转换 | 负显示器原点、窗口边界、留边矩形和 Retina 2× 元数据换算 |
| 长轨迹 | 合成 10 分钟、60 Hz 鼠标数据，评估 18,000 帧，镜头值有限且不越界；这不是 10 分钟真实录屏 |
| 工程数据 | 拒绝越界区间、路径穿越、不支持版本与不可表示时长；工程包移动后可重开 |
| 视频导出 | 真实编码并解码 4 秒合成素材，删除 1 秒后 MP4 为 3 秒；检查尺寸及聚焦目标像素 |
| GIF 导出 | 实际 GIF 帧数与代表帧正确，聚焦区域与 MP4 一致 |
| 音频同步 | 生成已知时间脉冲，实际剪辑导出后片段重排并裁剪后脉冲在 0.5 秒与 2.5 秒，误差小于 100 ms |
| 取消导出 | 开始前取消、GIF 渲染中取消；原目标文件不变，本次临时文件清理 |
| 素材保护 | 通过指向工程媒体目录的符号链接导出也会拒绝覆盖原素材 |
| 保存等待 | 结束操作等待现有保存和导出清理，不因再次调用停止而提前退出 |
| 启动失败 | 保留早期错误；旧录制代次回调不能影响新代次 |
| 静止尾段 | 实际写入一帧并在 2 秒停止，导出保留静止尾段；未测试编码器持续背压 |
| 录制恢复 | 将可读但未完成工程元数据的素材恢复为可打开工程 |

## 原生界面检查

- PASS：首页、录制入口、资料库和编辑器布局已通过截图检查。
- PASS：系统拒绝录屏时显示错误和设置入口，关闭提示后应用恢复可操作。
- PASS：打开合成示例工程，聚焦片段设置可见。
- PASS：设置 0.5 秒起点、保留选区后时长从 3 秒变为 2.5 秒；撤销后恢复 3 秒。
- PASS：从界面选择 GIF，保存为 `dist/ui-export.gif`，观察到进度与导出完成提示；文件存在。
- PASS：退出后启动最终构建，从最近工程重新打开同一示例。

## 尚未完成的真实设备验证

系统在获取屏幕/窗口列表时返回 TCC 拒绝，未自动修改用户的权限。已向用户说明开启录屏权限的路径，当前未获得已开启的回复。

以下项目为 NOT RUN，不应作为已通过对外承诺：

- 屏幕、单窗口及真实麦克风录制与音画同步。
- 实际 ScreenCaptureKit 帧的 Retina / contentRect / scaleFactor 数值核对。
- 实际窗口移动、改尺寸、关闭、缩小到后台等情形。
- 多显示器与混合像素密度屏幕。
- 真实 10 分钟录制、持续编码器背压、长 GIF 内存趋势。
- 真实录制启动/停止过程中退出应用的端到端系统测试；等待逻辑已有自动化覆盖。
- 第二台 Mac、Intel Mac、Developer ID 签名与公证。

## 独立代码审查

一次只读审查指出 5 项重要问题：Retina 单位不一致、保存中退出竞争、超大有限时长崩溃、符号链接绕过原素材保护、启动阶段吞掉错误。已修复并增加相应测试，完整测试套件通过。审查后的修复使用本地测试验证，未声称做过第二轮独立审查。

## 交付定位

这是可运行的本机测试版，不是已经完成真实录屏验收、正式签名和团队分发的发布版本。源文件与工程均保留在本地独立仓库中，没有上传到远程服务。

## v0.2 时间轴扩展

- PASS：按帧分割、拒绝边界碎片、双向重排、重排后的源时间映射与选区裁剪。
- PASS：吸附阈值、聚焦边界约束、跨切点聚焦局部修改、未修改片段的自动跟随动画保持一致、撤销历史。
- PASS：使用实际编码的彩色测试视频，重排后分别解码 MP4/GIF，核对片段顺序及聚焦目标像素；音频脉冲同步验证通过。
- PASS：新版时间轴及聚焦手柄完成原生截图布局检查。
- NOT RUN：受控的原生鼠标拖拽全过程验证。用户正在操作应用，自动化拖拽被中止，后续没有抢占用户输入。核心编辑行为已有自动化覆盖，但不等同于手势端到端验证。
- 独立只读审查未发现阻塞问题。提出的分割后片段高亮与选区不一致问题已修正为同时选中右侧片段。
- 新应用单独放在 dist/v0.2，避免覆盖用户正在运行的版本。新版编辑工程使用格式版本 2，可读取旧工程；旧版应用不能读取版本 2 工程。
- 上述真实录屏、第二台 Mac 与公证限制仍然适用。
